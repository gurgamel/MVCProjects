
<!--This is the defaul view. It uses the default layout.
    The default layout has a space for RenderBody where it renders the 
    specific views called by controller methods.
    This view and its layout are always called as a wrapper around the views called from 
    specific controller methods.-->


@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}